---
msmtp_package: msmtp
msmtp_package_state: "{{ apt_package_state | default('present') }}"
msmtp_config_mode: "0644"

msmtp_server: "{{ mail_smtp_server }}"
msmtp_port: 587
msmtp_from: "{{ mail_admin }}"
msmtp_domain: "{{ mail_hostname }}"

msmtp_auth: "on"
msmtp_user: "{{ mail_user }}"
msmtp_password: "{{ mail_password }}"

msmtp_tls: "on"
msmtp_tls_starttls: "on"
msmtp_set_from_header: "on"
msmtp_allow_from_override: "on"

# msmtp_accounts:
#   - name: account_name
#     host: smtp.example.com
#     port: 587                          # optional, defaults to msmtp_port (from msmtp_server)
#     from: user@example.com
#     user: user@example.com
#     password: "{{ vault_smtp_password }}"
#     auth: "on"                         # optional, defaults to msmtp_auth
#     tls: "on"                          # optional, defaults to msmtp_tls
#     tls_starttls: "on"                 # optional, defaults to msmtp_tls_starttls
#     domain: example.com                # optional, defaults to msmtp_domain
msmtp_accounts: []
