---
- name: Install msmtp
  apt:
    name:
      - "{{ msmtp_package }}"
      - msmtp-mta
    state: "{{ msmtp_package_state }}"
    cache_valid_time: "{{ apt_cache_valid_time | default(3600) }}"

- name: msmtp configuration
  template:
    src: msmtprc.j2
    dest: /etc/msmtprc
    mode: '0600'
